'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
      <button className='btn btn-primary' onClick={ () => alert("This is just a test.") }>
        Product
      </button>
    </div>
  )
}

export default AddToCart
